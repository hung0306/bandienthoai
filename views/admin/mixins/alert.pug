mixin alert-success(time)
    - if (messages.success)
        .message.info
        div(data-time=time show-alert)
            span= messages.success

mixin alert-error(time)
    - if (messages.error)
        .message.info
        div(data-time=time show-alert) #{messages.error}
            span(class="ml-5") x